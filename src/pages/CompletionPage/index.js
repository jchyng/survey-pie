import styled from 'styled-components';

const ComponentPageWrapper = styled.div`
  padding: 4em;
  background: aqua;
`;

export default function CompletionPage() {
  return <ComponentPageWrapper>CompletionPage</ComponentPageWrapper>;
}
